# need some sort of identifying info + authentication
/clock:
    post:
        description: Clock in
        operationId: clockIn
        responses:
            '200':
                description:
                application/json:
                    schema:
                        success: boolean
            '4XX':
            
            default:
                description: default behavior/err
                content:
            
    put:
        description: Clock out
        operationId: clockOut
        #...