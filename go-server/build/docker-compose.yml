version: '3.1'

services:

  db:
    image: stack-postgres
    restart: always

  # provides web interface to db
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080